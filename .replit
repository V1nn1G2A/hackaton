run = "cd backend && npm install && npm run build && node dist/main"

[deployment]
run = ["sh", "-c", "cd backend && node dist/main"]
build = ["sh", "-c", "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build"]

[[ports]]
localPort = 5000
externalPort = 80
